/**
 * Production environment configuration.
 * Replace apiGatewayUrl with the production API Gateway address before deploying.
 */
export const environment = {
  production: true,
  apiGatewayUrl: 'http://localhost:8765'  // Update for production deployment
};
